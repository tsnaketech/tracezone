# Astro's build output and the Vercel adapter's function bundler both resolve
# dependencies by walking up from the build output directory, which pnpm's
# default isolated layout cannot satisfy without symlinks. A flat tree keeps
# `pnpm build` behaving identically on Linux, macOS and Windows.
nodeLinker: hoisted

onlyBuiltDependencies:
  - esbuild
